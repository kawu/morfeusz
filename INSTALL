To install Morfeusz2 Haskell bindings, you will have to install Morfeusz2 in your system beforehand.
Otherwise you can consider using extra parameters as in the example below:

$ stack ghci --extra-include-dirs=$MPATH --extra-lib-dirs=$MPATH

where $MPATH is a path to Morfeusz library.
